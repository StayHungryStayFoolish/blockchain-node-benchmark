# Runtime fixtures are part of the deterministic local closed-loop test suite
# and should be committed when they represent sanitized fake-node responses.
#
# Large per-method request/response audit evidence lives under
# docs/audit/rpc-fixtures/<chain>/<method>/ and is ignored at the repository root.
