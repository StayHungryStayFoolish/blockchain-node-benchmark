global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: blockchain-node-benchmark
    metrics_path: /metrics
    static_configs:
      - targets:
          - exporter:9108
